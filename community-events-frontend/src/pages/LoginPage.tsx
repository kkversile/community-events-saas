import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { Building2, LockKeyhole, Smartphone } from 'lucide-react'; import { useAuth } from '../auth/AuthContext'; import { errorMessage } from '../api/client';
export default function LoginPage(){const {login}=useAuth();const nav=useNavigate();const [communityCode,setCommunityCode]=useState('VSRES'),[mobile,setMobile]=useState('9000000209'),[password,setPassword]=useState('Resident@123'),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setError('');setLoading(true);try{const user=await login(communityCode,mobile,password);const admin=user.roles.some(r=>['SUPER_ADMIN','COMMUNITY_ADMIN','TREASURER','EVENT_ADMIN'].includes(r));nav(user.mustChangePassword?'/change-password':admin?'/admin/dashboard':'/app/dashboard');}catch(err){setError(errorMessage(err))}finally{setLoading(false)}}return <div className="login-page"><div className="login-panel"><div className="login-logo">CH</div><h1>CommunityHub</h1><p>Events, contributions and family participation without WhatsApp chaos.</p><form onSubmit={submit}><label>Community code<div className="input-icon"><Building2 size={18}/><input value={communityCode} onChange={e=>setCommunityCode(e.target.value)} /></div></label><label>Mobile number<div className="input-icon"><Smartphone size={18}/><input value={mobile} onChange={e=>setMobile(e.target.value)} /></div></label><label>Password<div className="input-icon"><LockKeyhole size={18}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></div></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary btn-block" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><div className="auth-links"><Link to="/register">Register flat 103 / 104</Link><Link to="/forgot-password">Forgot password?</Link></div><div className="demo-logins"><strong>Demo logins</strong><button onClick={()=>{setMobile('9000000209');setPassword('Resident@123')}}>Resident A-209</button><button onClick={()=>{setMobile('9000000001');setPassword('Admin@123')}}>Community Admin</button><button onClick={()=>{setMobile('9000000002');setPassword('Treasurer@123')}}>Treasurer</button></div></div></div>}
