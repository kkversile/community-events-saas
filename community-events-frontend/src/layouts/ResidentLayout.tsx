import { Outlet } from 'react-router-dom'; import { LogOut } from 'lucide-react'; import { useAuth } from '../auth/AuthContext'; import PushNotificationsPrompt from '../components/PushNotificationsPrompt';
export default function ResidentLayout(){const {user,logout}=useAuth();return <div className="resident-shell"><header className="resident-header"><div><strong>{user?.community.name}</strong><span>{user?.unit?.displayName??'Resident'}</span></div><div className="action-row"><PushNotificationsPrompt/><button className="icon-btn" onClick={logout} title="Logout"><LogOut size={19}/></button></div></header><main><Outlet/></main></div>}
